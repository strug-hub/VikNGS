version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# Paths are relative to the repo root. The docs live under docs/ after
# vikngsdocs was merged in as a subtree.
sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/docs/requirements.txt
